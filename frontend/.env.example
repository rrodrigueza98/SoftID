# En desarrollo local no hace falta (Vite proxea /api al backend, ver vite.config.ts).
# En Vercel, configurar esta variable con la URL pública del backend en Railway,
# SIN barra final y SIN /api al final (se agrega solo):
#   VITE_API_URL=https://tu-backend.up.railway.app
VITE_API_URL=
